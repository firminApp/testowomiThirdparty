<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.firminapp.owomithirdparty">

    <application>
        <receiver
            android:name=".recevers.OperationResponseRecever"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.hesystems.owomi.modules.OPERATION_RESPONSE" />
            </intent-filter>
        </receiver>

        <activity android:name=".sdk.TestActivity"></activity>
    </application>

</manifest>